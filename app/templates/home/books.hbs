{{page-title "Books"}}
<div class="container">
<div class="container-body">
    <TableHeader @title="Books" @onAdd={{this.openModalToSave}}></TableHeader>
    <DTable @headers={{this.headers}} @onEdit={{this.openModalToEdit}} @onDelete={{this.deleteBook}} @dataList={{@model}}></DTable>
</div>

</div>

<BookModal @book={{this.book}} @opt={{this.opt}}></BookModal>